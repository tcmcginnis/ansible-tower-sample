# ansible-tower-sample

Testing my first AAP deployment 

<b>
  
Hello world yaml.   


kubeinfo.yml
add a variablle named "kube_contents" which contains the base64 encoded contents of the kube-config

   cat ~/.kube/config | base64 --wrap=0
