- hosts: localhost
  user: root
  tasks:
   - name: Get the cluster infrastructure information
     command: find /
     ignore_errors: yes
     register: command_output

   - name: Report
     debug:
       msg: "{{ command_output }}"

